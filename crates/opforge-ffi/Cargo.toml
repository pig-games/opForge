[package]
name = "ffi"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
api = { package = "libopforge", path = "../opforge-lib" }
