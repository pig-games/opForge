[package]
name = "ffi"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
name = "opforge"
crate-type = ["rlib", "cdylib"]

[features]
panic-test-hooks = []

[dependencies]
api = { package = "libopforge", path = "../opforge-lib" }

[dev-dependencies]
cc = "1"
libloading = "0.8"
