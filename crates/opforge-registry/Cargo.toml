[package]
name = "registry"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[dependencies]
opcore = { path = "../opforge-core" }
package = { path = "../opforge-package" }
types = { path = "../opforge-types" }
