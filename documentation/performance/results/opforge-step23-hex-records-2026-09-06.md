# Step 23 — sparse native HEX records

The native HEX writer now follows the existing image presence map, splits at gaps and at Rust's 32-byte record limit, and preserves BIN gap materialization. It uses no new storage. A pre-write bound reserves each record and the final EOF within the existing 12,000-byte artifact buffer. Internal branch labels are private; the public entry and following declarations remain accessible.

The complete test emits five bytes at $8000 and 33 contiguous bytes at $8006. Its live Rust oracle requires HEX records of 5/32/1 bytes plus EOF and an exact39-byte BIN with a zero-filled gap. Independent register, pointer, branch and buffer-bound review passes; host oracles/source contracts pass. Final native confirmation passed in63.56seconds with fresh protocol, explicit zero exit and exact live Rust HEX/BIN bytes.

The first attempt stopped during host assembly because SUBI's symbolic immediate lacks the required Hunk relocation form. LEA plus register subtraction preserves the same check using supported relocation. The second attempt completed with guest exit1 and a tokenizer overflow:33 values on one line exceed the documented64-token limit. The fixture now uses two directives containing16 and17 values, preserving every address and the same expected HEX/BIN bytes. Both failed receipts remain; neither is promoted to parity. No acceptance of the oversized single line is claimed, and B09's frozen source is unchanged.

Step performance contribution: output correctness enables qualification, with no measured runtime speedup. Total current Phase A runtime gain remains unmeasured. Prior measured mechanisms remain separately scoped, including Step19's1,608-to-8 DOS reads and Step22's corrected B09 reservation25-to-13 bytes. These are not additive runtime percentages.

Full B09 subsequently checked all eleven artifacts: nine are exact, including HEX. Map and listing remain mismatches. The64.149808834-second failed-case invocation is not a speedup measurement. The first non-LSP Rust gate passed 1,600 tests but failed one obsolete structural assertion expecting 255-byte HEX records. Updating it to the implemented 32-byte limit passed the focused test. The corrected full gate passed with exit 0 in 267.816852417 seconds (SHA-256 `8e522bfe4ac2a5fa69b4413d390afaa1795504e229a3f76027c9809746a702df`). Workflow, staged native, formatter and independent compliance checks pass. Phase A remains open.
