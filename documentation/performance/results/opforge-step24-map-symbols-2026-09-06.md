# Step 24 — canonical native map symbols

The native map writer emits canonical engine names directly. Root-module public visibility matches the separately recorded owner prefix and raw export name in place. A lexical successor traversal replaces the 32-bit visited mask, retaining original index order for folded-name ties without new state or a label-count ceiling below the existing engine capacity. The traversal remains quadratic, as before.

The complete proof source has 36 labels in scrambled definition order, with public labels at indices 1 and 33 and private labels on both sides of the old cutoff. Live Rust checks require exact all/public maps and a 36-byte BIN. The initial map-only native command completed with exit 1 and OPC-NCLI007 before assembly. The corrected command explicitly selects BIN in both Rust and native while preserving all source bytes and both map requests; it makes no map-only support claim. That failed receipt remains recorded. Corrected Level A/B checks pass. Fresh Level D passed in 64.03 seconds with explicit zero guest exit and exact BIN plus both maps. The non-LSP Rust gate passed in 261.510519375 seconds, exit 0, SHA-256 `8ccd570298c3918fc22d1853462ee4b00b24bdce7107117cbbcfa182c006266e`. Workflow, staged native, formatter, architecture and independent review passed.

Unchanged B09 checked all eleven artifacts: ten are exact, including the map. The listing remains a mismatch. Its first divergence is the missing active-section annotation, assigned to reviewed Step 25. B09 invocation duration is a failed-case validation receipt, not a runtime comparison.

Step contribution: canonical map parity and removal of the 32-label traversal limitation enable qualification. No runtime improvement has been measured for this step. Total current Phase A runtime gain remains unmeasured without a matched initial/current comparison. Previously measured DOS-read, focused runtime and validation-throughput gains remain separately scoped. Phase A remains open.
