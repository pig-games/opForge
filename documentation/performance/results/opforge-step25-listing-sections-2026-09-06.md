# Step 25 — section-aware bounded native listings

The listing renderer derives each source record's post-directive section from the first statement belonging to a later source record. A monotonic cursor uses existing collection indices, so blank/comment records and repeated source line numbers do not need a new projection table. The last record uses actual retained final section state. Semantic pre-dispatch ownership remains unchanged for labels, fixups, reachability and image routing. No BSS or per-statement capture was added.

Appending annotations exposed an existing unchecked-write hazard. Listing-only wrappers now reserve complete formatted spans, while direct byte writes are bounded by the existing 24,000-byte limit. Source text and section names reserve their known lengths once rather than checking every copied byte. Other artifact builders retain their existing helper contracts.

The first fresh native positive test failed with OPC-NCLI044 and OPC-NCLI023. Isolated host Hunk probes verified LEA addends, both relocation entries, reserve arithmetic/CCR instructions, pointer comparisons and a private call. These small-context checks did not explain the full binary. One reviewed, temporary Level E debug-framework event then recorded cursor offset 193, end offset 3,221,249,432, source record 0 and raw D0 0, under fresh completion and explicit guest exit 1. The end should have been 24,000. The renderer now derives the end directly from the loaded start pointer using MOVEA plus numeric ADDA. The general full-Hunk expression cause remains unproven. All temporary probe code, IDs, export and test were removed; failed and diagnostic receipts remain.

The complete positive fixture requires exact live Rust BIN and listing, including section open, blank/comment, data, and close rows. Separate Rust boundary-label proof preserves opening-label value 0 and closing-label value $8003. A complete oversized listing requires a fresh explicit native failure with the artifact-build diagnostic. Corrected native proof passes all six focused tests without a skip: exact BIN/listing, explicit oversized-listing rejection, the Rust and source contracts, the exact-fit model, and the retained isolated Hunk discriminator. The receipt has exit 0, 129.75 seconds, 1,207 bytes and SHA-256 `02466c03e0e25f8fe671eeacc630a59eec6abdc3f961797687162f61730a6095`. The workflow gate also passes with exit 0; its 7,044-byte receipt has SHA-256 `e3e7dcf656369537f8563e66e51a254c2b90e87e6f4af98ad269f795bac8e13c`.

Fresh B09 completed in 67.95852954080328 seconds and checked all eleven declared artifacts. Ten are exact. The listing alone differs: its first mismatch is offset 464, where native advances to source line 7 while Rust includes the `.pub` record at source line 6. This is a new, precisely localized listing difference; B09 remains failed and Step 25 does not claim to repair it. The retained 27,602-byte log has SHA-256 `bb64ca20f2beba1a95e0a31bf2b5560c8b5e1fac91273cf5d1394bc8967629a2`.

The first non-LSP Rust gate is also retained as failed evidence. It passed 1,609 assembler tests and failed only `native_item38_full_product_import_budget_contract`: the new layout import changed the measured graph from 419 to 420 while the assertion still expected 419. The run exited 101 after 243.08396791713312 seconds; its 208,017-byte log has SHA-256 `0d898d491c3a986eb0a2d9e3fa3085f9557b0165d37f957f118da7108c7dd327`. A focused corrected import-budget check passes at 420. The corrected full non-LSP Rust gate passes with exit 0 in 254.22 seconds (SHA-256 `493229c81d9879c7cb5b1604dfed246e308c279b3252ab684d1f3b893367d088`, 332,939 bytes); LSP remains deferred. The refreshed staged workflow gate passes after the import snapshot and plan amendments (SHA-256 `6bf982b6ebfefd31f8c4372f37dde88f8538ff19198b19dac5e91fef294f76b9`, 7,032 bytes).

Step performance gain is **unmeasured**: this correctness repair has no matched timing experiment, adds no storage or execution-time capture to ordinary assembly, and confines extra work to requested listing generation. Total current Phase A runtime gain remains **unmeasured** because no matched initial-to-current series exists across the same workload, mode, package and measurement scope. The retained cumulative mechanism result is Step 19's matched 22-line source fixture: **1,608 to 8 DOS reads, 99.50% fewer**, with a 4.2048% median START-to-DONE reduction in that fixture. This is not a whole-product runtime total, and unrelated step percentages are not added or multiplied. Phase A remains open.
