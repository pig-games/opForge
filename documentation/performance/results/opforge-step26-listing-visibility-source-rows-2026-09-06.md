# Step 26 — retain visibility directive source rows

The native CLI consumed `.pub` and `.priv` before its source-record boundary. Step25 B09 exposed the first omission at listing offset 464: native advanced to line 7 while Rust retained `.pub` at line 6. The corrected path updates existing visibility state, records the original source row, propagates recording failure, and returns consumed without creating a statement or routing metadata to the package. Existing record capacity and storage are unchanged.

Rust authority is `engine.rs` listing capture and `ListingWriter::write_line`. Native authority is `line_processor` through `assembly_session.opforgeNativeCliRecordSourceLine`. Independent production review confirms source ordering, register/CCR behavior and fail-closed propagation. Focused host and complete fresh native proof pass: three tests, including exact BIN/listing under a fresh zero-exit guest without SKIP (64.32 seconds for the filter). The complete native fixture deliberately has contiguous bytes and no labels, isolating source retention while still requiring exact whole listing/BIN equality; a separate host oracle covers public/private labels.

Step runtime gain is unmeasured: this repairs correctness and adds only the required retained source rows. Total Phase A runtime gain is also unmeasured without a comparable initial/current benchmark. The separately verified Step19 source mechanism reduced DOS reads from 1,608 to 8 (99.50% fewer); this is not a cumulative runtime percentage. All remaining B09 and Phase A debt remains open.

Fresh B09 checks all eleven artifacts and keeps ten exact. Its listing first mismatch is offset 1260: the BSS reservation row 19 shows `----` in native versus an address in Rust. This precedes the symbol footer at native offset 2507. Both visibility source rows are retained; the next independently reviewed repair must address reservation listing status/address/auxiliary size before the later known footer differences. The failed B09 receipt is retained, not promoted to parity or a timing result.

The required non-LSP Rust gate passes with exit 0 in 264.400876 seconds (SHA-256 `d651c92a91ec57cb4c8135b01b962c23b76019cbce4adbf243cef7075257619f`, 333,370 bytes). Staged workflow passes with exit 0 in 30.985681 seconds (SHA-256 `d7582681094d2364088bc656202f0bc6f597276dfd6816d0bd5cd648b2697149`, 6,494 bytes), including formatter, architecture, budgets and slice checks. LSP remains deferred.
