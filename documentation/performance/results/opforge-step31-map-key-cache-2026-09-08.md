# Step 31 Rust map-key cache result — 2026-09-08

The one-expression `build_mapfile_text` change is qualified on the bounded actual-function discriminator, pending focused commit. All four candidate map artifacts are byte-identical to baseline.

The VM library uses the Cargo test profile with opt-level=1 and debug assertions/overflow checks enabled; the standalone harness also uses opt-level=1. These are development/test-profile gains, not release-build timing. The harness prebuilds the `SymbolTable`, directive, regions and sections. Three pairs run an already-linked baseline executable followed by the candidate executable in fixed order; each run warms once and records five samples of ten calls. Median-of-run-medians improved from 22,279.2ns to 6,504.2ns per N=36 All call (70.806%), 17,066.6ns to 3,837.5ns for N=36 Public (77.515%), 1,865,787.5ns to 687,162.5ns for N=4096 All (63.170%), and 1,396,075.0ns to 363,175.0ns for N=4096 Public (73.986%). Timing is a local microbenchmark, not whole-CLI or native runtime evidence.

A separate System-backed counting allocator resets after fixture construction and measures one function call. Allocation events fall from 706 to 153 (N=36 All), 624 to 71 (N=36 Public), 68,868 to 16,400 (N=4096 All), and 59,650 to 7,182 (N=4096 Public). These totals include output formatting; no timing claim comes from counted builds.

Exact output SHA-256 values remain `1560dae61dd56ababcf3f8093e4f92d9cd508f5b1ffaf50fb99cf783f6d1f0b7` (36 All, 1,178B), `a63cde0e066fad513e547c341cb66898c9f5af0d65e381b6bca096ed02e5ed0c` (36 Public, 368B), `e5246e5bc9e90c85f1f95da333cbb644e40d6c9e587ac4d66fd3aba7906960e3` (4096 All, 121,963B), and `5615c562142022084e8794c4415cf240f21acb6ad039943d36b06714be78da34` (4096 Public, 29,803B).

Cached lowercase keys coexist during sorting. Rust 1.95 `alloc/src/slice.rs` documents a temporary `Vec<(K, usize)>` of length N; the local size probe reports a 32-byte `(String, usize)`. The gross requested-storage bound is therefore `32*N + sum(lowercase-key UTF-8 bytes)`: for the fixed 16-byte names, 1,728B at N=36, 196,608B at N=4096 and 3,168,000B at the 66,000-entry table limit. This excludes allocator bookkeeping/rounding and unchanged input references/output; it is a derived structural bound, not a measured whole-function peak or net peak delta. Authority/probe sources and hashes are embedded in the JSON. The original first timing attempt reconstructed the table inside each call; it is retained in `/private/tmp/opforge-step31-map-key-baseline/timing-end-to-end-original.txt` and excluded. The JSON embeds both harness sources, compilation commands and all six timed invocation/redirection commands; it hashes the static-linked old/new executables and exact baseline/candidate production sources. The baseline source is retained from commit `24efe391` with SHA-256 `2480d93968e85fd849ebe276fe10ae9c890b3311d23c3cd21eb58bda7b703426`; its overwritten rlib is not claimed as retained. Reproducible sources, commands, compiler/build receipts, raw samples and exact outputs are retained under `/private/tmp/opforge-step31-map-key-baseline` and `/private/tmp/opforge-step31-map-key-candidate`.

Cumulative context remains unchanged from the earlier Step19 source-buffer proof: native source DOS `Read` calls fell from 1,608 to 8 (99.5025%). Step31 does not contribute to that count. Total Phase A runtime gain remains unmeasured, and this Rust-only slice claims no native gain.

The timing order was fixed baseline then candidate, not counterbalanced, so ordering bias remains possible. Retention relies on the large allocation reduction, non-overlapping practical timing ranges, exact executable/source identities and exact artifact equality rather than timing alone.

Focused map tests pass (2 tests, 0.5357s; log SHA `dbaf2283…`), the non-LSP Rust gate passes (299.1951s; `073e6fe1…`), workflow passes (30.4377s; `97e9482f…`) and the plan wrapper passes (0.1069s; `0224ea18…`). Exact receipts are embedded in the JSON. Independent final compliance by `step28_scalar_review`: PASS; final metadata plan and checkbox validators also pass.
