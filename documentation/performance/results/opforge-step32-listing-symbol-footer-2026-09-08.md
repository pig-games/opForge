# Step 32 native listing-symbol footer result — 2026-09-08

Status: focused host, representative listing, placed-address listing, and unchanged all/public map Level D proofs pass. B09 checks all 11 artifacts and retains one listing mismatch in the separate generated/ORG footer debt.

The final representative source is 260 bytes (SHA-256 `5315205e…`) and the placed-address matrix is 531 bytes (`1bfd6e75…`). Each actual case selects `--bin` and `--list`, uses CPU 68020 and the shipped package `4d6a03d5…`, and owns its complete live Rust oracle. Both exact BINs are `AA`; the listing oracles require canonical untruncated root names, Rust uppercase-key order, public/private `lbl` rows, and placed 4/6/8-digit values. The representative exercises a spaced label colon followed by a comment colon and an unspaced label colon; a separate actual `ListingWriter` Level A test covers indentation, label-only and immediate-comment forms, identifier continuation punctuation, and unchanged directive/comment/quoted-operand colons.

The retained first representative source was 243 bytes (SHA-256 `e0418c29e4f6d1b387760b2dc8d00a70f4e35f7fa7b47bbd69665f47adb46e63`) and used `_private_name_longer_than_fifteen` as a source label. Its fresh guest completed with exit 1 and `OPC-NCLI038`/`OPC-NCLI039`/`OPC-NCLI010`; `captureInvocationLabel` accepts an alphabetic first character, so the run failed before listing footer execution and is not footer evidence. The corrected internal underscore forms `a_private...` and `z_high...` preserve the intended uppercase-key versus lowercase-key order discriminator while satisfying the existing source grammar.

The second fresh representative used the valid 244-byte source and completed with explicit guest exit 0 and exact BIN, but its listing was 1,343 bytes against Rust's 1,341. The first mismatch was native `:` (`0x3A`) against Rust space (`0x20`) at offset 630; inspection confirmed only the two leading label colons differed. This localized Rust's `normalize_leading_label_colon` contract and motivated the bounded source-row normalizer. It was not a listing parity pass.

The final representative and placed-address runs each completed a fresh protocol with explicit guest exit 0 and byte-exact BIN/listing artifacts. The unchanged all/public map case independently completed with exact artifacts and exit 0. B09 then checked every one of its 11 declared artifacts: ten matched, while only `opforge_6502_native_cli_smoke.lst` differed. Native reported total memory 9 against Rust 8 and omitted Rust's final generated-output row; the first mismatch was offset 2,686. This confirms the Step32 symbol footer inside B09 while retaining generated/ORG footer closure as separate debt.

Level A and both focused Level B contracts pass. Host invocations of Level D tests skipped for absent FS-UAE configuration and are not native proof. The first assembly attempt failed because the new module lacked required closing directives. The corrected second assembly passed before the final normalizer; the final third assembly and second formatter checks pass. Earlier capacity attempts are retained stale-snapshot failures; the final fifth run passes all six checks at 1,729,024 loadable source bytes, 94,706 processed rows/3,582,080 bytes, 426 imports, and 6,506 public declarations/127,952 name bytes. The fresh guest rebuilds are the final-code binary authority.

The listing successor traversal remains O(N²). The explicit callback adds one four-byte saved pointer and helpers use bounded register-save frames; no persistent storage was added. Step runtime and total Phase A runtime remain unmeasured. Cumulative context is unchanged: the Step31 test-profile map microbenchmark retained 63.17–77.51% gains, and the earlier source-buffer work retained 1,608→8 native source DOS reads (99.5025% fewer). Step32 claims neither gain.

The final native source adds 4,788 loadable bytes and 217 processed rows/4,569 processed graph bytes relative to the prior snapshot. These are source/storage costs, not runtime measurements. Historical Step10, Step13 and Step14 timings use different workloads and are not combined into an initial-to-current series.

The first full non-LSP gate retained three stale host-contract failures after 1,641 passes. The tests still named listing V1, expected 100 staged files instead of 101, and expected the removed raw `sourceLoop`. After binding them to V2 plus its callback, the measured staging set, and the bounded source appender, all three focused checks pass. The single final non-LSP gate passes in 275.576 seconds; the failed first gate remains recorded.

This slice covers root-owned immutable PC-backed labels. Mutable `var`/`set` kind metadata, non-root/imported public visibility, generated/ORG footer debt, unrelated B09 artifacts and Phase A closure remain open. Exact identities and retained receipts are in the adjacent JSON.
