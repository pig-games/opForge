# Step 35 compact zero-operand memo — qualified B01/B03 evidence — 2026-09-08

Status: the one-entry zero-shape compact-table memo has positive, stable B01 timing, exact focused lifecycle and malformed-input native proof, a successful B03 confirmation, and a passing final Rust quality gate. B10 remains an explicitly open wider-corpus debt after a fresh post-START timeout. Staged-native, workflow, artifact and plan checks pass. Final independent compliance passes; only the focused commit and safe integration remain pending.

The frozen B01 case `114ecc1e524a209c1756dd4cfec5cbcc010cbe483f17097bc5eefa8b4d45057d` used shipped package `4d6a03d5718bc9380f02452294c404ec82e4e3716471ad5c95b349c8c6e25872`, profile off, longword/live/buffered modes, a 120000 ms post-start limit, and 250 ms polling. All six alternating reference/candidate runs completed fresh protocol with explicit guest exit 0 and exact `Work/output.bin`. The uncached image was consistently `fnv1a64:2ee18e29474c76c5`; the memo image was consistently `fnv1a64:018cba33cd59bc8d`.

Uncached START-to-DONE samples were 36.996669833, 37.234600708, and 36.914197583 seconds: median `36.996669833`, range `36.914197583–37.234600708`. Memo samples were 34.447279375, 34.428058125, and 34.384722292 seconds: median `34.428058125`, range `34.384722292–34.447279375`. The median reduction is `6.9428186904%` (`2.568611708` seconds), above `max(5%, 1.3514729900%)`. Whole-test wall times, commands, environments, hashes, and all six raw receipts remain in the adjacent JSON.

Focused host proof first compiled but failed only because the mixed CLI fixture used unsupported column-zero/inline-label syntax; receipt `76121b871164ef1d86bdeb63496657c758082f774f56b614a04446624c4cd7ff`. After adapting the same instruction stream to accepted source syntax, all three focused host tests passed; receipt `3ed084296db1b4a7cce8e92721505fa94fef86598aa97feedadbb9e0a9ae7ef5`. The live Rust oracle decodes a forced CTBL package, resolves aliases and pipelines through `RuntimeModelCore`, executes encode candidates, and maps each one-byte emission back to one unique decoded CTBL program. Its 24-row dynamic batch covers bounds/no-match requests, repeated and case-varied zero keys, mnemonic changes, shaped-mode bypass without eviction, over-32 mnemonic fallback, over-64 mode fallback, successful CPU change, failed selection preserving context, an in-bounds shifted same-address valid-package reload, failed load, and post-failure lookup. The mixed CLI case uses the live Rust CLI artifact `ea a9 12 ea d0 01 ea ea 60`.

The focused native run executed both actual guest cases from that host-owned proof: the 24-row lifecycle batch and mixed CLI artifact both completed fresh protocols with explicit guest exit 0 and byte-exact live Rust outputs; receipt `a62f2151546793da76b40207e0da567bc62efa9233f04e4969c460aa069a107c`. The unchanged malformed compact fixed-opcode test then executed two additional actual guests for version and opcode mutations; both completed with the expected rejection evidence and receipt `17819a3aab41982b74af47a2344b3776e9a3bc3796962d1e352d0a88e8eff8c1`. These focused runs prove the enumerated request, lifecycle, output, and malformed-input cases only; they do not infer wider corpus completion.

B03 candidate confirmation completed fresh protocol with guest exit 0 and exact `Work/output.bin` in `37.735005375` seconds. Its case hash is `be04f9426a0ffb55278cdb05d1d604f5895e85a2cee0b6894ac43c9df1359acf`, native image is `fnv1a64:018cba33cd59bc8d`, and receipt is `4b0405c8b1d107f24a6cf157dd18528ff78347656a74de25b0be5669fe937889`. The prior Step 34 B03 timeout remains historical failure evidence, so there is no valid B03 reference/candidate speedup ratio.

B10 candidate produced a fresh START marker but no DONE marker, result, guest exit, or exact artifact proof before the 120000 ms post-start limit at 250 ms polling; receipt `76f5035e3f60113161dfcced990749b02efe13150842ec3612b6de0b36ea7f6b`. Its package and source aggregate identities matched this step, but the failed run supplied no native image digest. B10 completion remains open wider-corpus debt and is not required to retain the mechanism on the qualified B01/B03 evidence.

Independent source review and both candidate/reference assemblies passed. Assembled layout proves a 40-byte memo payload plus 3 alignment bytes, adding 43 bytes to the package-state clear/BSS range, with 8 additional helper-stack bytes. `PackageStorage` is at control-block offset 12,812, below the 65,536-byte v1 limit; the new clear range is 2,752 bytes (`$0ac0`). The focused six-test capacity suite passes at 1,732,309 loadable bytes, 94,827 rows/3,585,200 processed bytes, 426 imports, and 6,516 public declarations/128,183 name bytes.

The final Rust quality gate passed with `scripts/workflow/run_rust_quality_gate.sh --defer-lsp` in 273.45939199998975 seconds; receipt `bc836c1e988e710f89d6b36e1b6cca9a62703fde57a655d62a072d0c6b593e52`, log size 344,064 bytes. Staged native, workflow, compliance, and commit work remains pending.

The prior 1,608→8 source DOS-read reduction (99.5025%) and Step 31 test-profile Rust map gains of 63.17–77.51% remain separate unchanged results. They are not combined with this native B01/B03 evidence. Total Phase A end-to-end runtime remains unmeasured.

Final validation retains the first workflow failure as a stale Item 5.2 inventory measurement. After refreshing only the compact-table and pipeline inventory entries, workflow, artifact bundle, plan workflow and cached diff checks pass. The single non-LSP Rust gate and staged-native gate pass; neither was repeated for receipt-only evidence updates.
