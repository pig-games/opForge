# Step 38 CLI longword clear — no-go — 2026-09-08

The byte-reference frontend screen recorded 706 ticks and the aligned-longword candidate 678 ticks: 3.9660056657% lower, below the required greater-than-5% threshold. The prototype and corpus selector are restored byte-for-byte to `cc19cbef`. No runtime improvement is retained. Focused documentation gates and independent compliance pass; only the focused documentation commit and safe integration remain pending.

Both B10 abort-after-one platform runs completed fresh controlled Level E protocol with deliberate guest exit 1, identical stdout/stderr, normalized non-timing profile counters and the same case/package/corpus/manifest. The companion JSON binds every command, environment, source hash, receipt, fresh protocol, duration and result/log hash. Whole-wrapper durations are recorded separately and are not speedup denominators.

The reference outer process overlapped a cached formatter check (epoch 1788868577.236–1788868581.968). The records do not establish the exact host-time guest window, so guest overlap cannot be excluded. This is an additional comparison limitation: the pair cannot establish a retained gain. The observed difference is already below threshold; no extra screen, repeated series or expanded proof was run. Future measurements must serialize all competing build/format work before launch.

Independent source/ABI review and both host assemblies passed (reference tool session 80504, candidate 69402, both exit 0). The canonical formatter arguments with the cached host binary passed, as did the architecture guard after escalation for its report-writing path. These establish reviewed source/assembly shape, not native memory/ABI execution or product parity. No actual-native nine-row matrix, B03, full B10, staged-native or full Rust gate ran for this rejected prototype.

Step gain is zero. The historical cross-revision B01 indicator remains 75.563602708 → 34.428058125 seconds: **54.4383% less elapsed, 2.1948×, 41.1355 seconds saved**. It is not a matched total Phase A speedup. Separate qualified fixture observations remain module buffering 23.462% less elapsed, source DOS reads 1608→8, and Rust map timing reductions 63.17–77.51%; they are not added or compounded. Latest complete B03 is 37.735005375 seconds with exact output; prior timeout is not a ratio denominator. Total matched product/Phase A runtime gain remains unmeasured.

All B10 and wider Phase A correctness/qualification debt remains open. The pointer returns to Step 39/48, A-close. Phase A's final report will include evidence-backed recommendations, then execution stops; Phase B/C0 requires a new instruction. LSP remains last at Step 48.
