# Step 39 — complete native session reset — 2026-09-08

Step39 is complete: focused native/product/capacity proof, the final non-LSP Rust gate, staged-native, inventory, workflow, artifact and plan checks, and independent compliance pass. The focused commit and safe local integration follow this recorded evidence.

The engine's factored header count now matches its 98 emitted bytes. Full and live-statement initialization each clear through the actual session end, including the final six image-presence bytes. The requested extent rises from 41,221,928 to 41,221,934 bytes. Allocations, field offsets, primitive behavior and live-statement clearing are unchanged.

The separate permanent native harness seeds the actual final six presence bytes with two different patterns across repeated initialization. It checks those bytes become zero, the pre-session `FlowPending` sentinel remains intact, first/last unused statement rows remain poisoned in live mode and clear in full mode, and CPU text/status/stack/A0/D1 behavior is correct. It writes no guard outside allocated memory. Both native modes completed fresh protocols and explicit zero guest exits. The existing source-driven Level C suites pass all 12 checks, including exact emitted extent, outer guards and whole live/full memory effects. These models do not substitute for native proof. Adding a separate harness avoids making every existing progress/profile test perform large session clears.

The native pair's receipt records pre-start timeout 120000ms but omitted the planned post-start setting, which defaulted to 300000ms. The entire serial process, including setup and both guests, finished in 115.072910s. Each guest therefore also completed within 120 seconds. Independent review accepted this focused correctness evidence without another run; the actual configuration is preserved, and no runtime comparison uses this pair.

B01 separately passed complete exact live-Rust BIN parity with guest exit zero, profile off, 120000ms post-start limit and 250ms polling. START-to-DONE was **34.43573325s**, native image `fnv1a64:d0404c095b1838a3`; case `114ecc1e524a209c1756dd4cfec5cbcc010cbe483f17097bc5eefa8b4d45057d`, package `4d6a03d5718bc9380f02452294c404ec82e4e3716471ad5c95b349c8c6e25872`. Whole-wrapper cost was 64.780061s and is not the native timing boundary.

Six source/capacity checks pass: 1,732,210 loadable source bytes, 94,826 processed rows and 3,585,101 processed bytes. Only exact measured snapshots changed; capacity limits were not raised. The 99-byte and one-row source reductions reflect the shortened comments/alias declaration, not a measured runtime gain. The companion JSON preserves the initial compile/module-path setup failures and stale capacity-snapshot failures as well as subsequent successful receipts; none was a guest failure.

**Step performance gain: none measured.** The current B01 observation is within one polling interval of the previous 34.428058125s median. The historical cross-revision B01 median indicator remains 75.563602708→34.428058125s: **54.4383% less elapsed, 2.1948×, 41.1355s saved**. It is not a matched total Phase A gain, and this single current observation does not replace that median. Separate module buffering, DOS-call and Rust map measurements are retained in the JSON and are not added or compounded. Total matched product/Phase A runtime gain remains unmeasured.

This repairs the E0-4 initialization extent invariant. E0-5's historical claim of stale CLI output was never demonstrated: both pass-entry paths already reset the presence map. The current proof establishes reset state, not a past output failure's cause. The unused legacy CLI size constants do not drive initialization; the CLI calls the corrected engine owner. B10 and every other Phase A debt remain open. After Phase A's complete verification and closing tally/recommendations, execution stops; later phases require a new instruction, and LSP remains last.

The single final non-LSP Rust quality gate passed in 812.522898s (log SHA-256 `4fbb369146aff20bbbdb7dadd6de6fdc1c26763609e46d52878a4c3a1c3d1a47`). It was observed on the same live process throughout; no timeout-driven restart occurred. LSP remains explicitly deferred. This gate duration is validation cost, not product runtime.

The final staged-native gate passed in 6.053336s (log SHA-256 `452cebf7f4e97035b9fb19493fd3b37f8ffe40e0c80da9545c6a01811ed1febf`). The first workflow gate correctly failed only because the engine source inventory SHA was stale (log SHA-256 `e448f30d935fa72160c621b8c53f8917c0d911ecc867e3dd2e1e2b324eae7fa2`). Refreshing that SHA left its audited surface unchanged at 106 routines, four imports, `code`/`bss` sections and 37 diagnostic paths; the focused inventory test then passed (`6abe9da26c1cd37fc617ec8a2f159fb9e60113988a7640866262fc12246b302b`). The corrected workflow gate passed in 28.461526s (`885e792933d71f0e2715fc99c5a0a557793a898a41c4ee97d285a342c3eddf47`), followed by artifact (`8f8915466fadb5eebd1849c0f610fbdffec71a663c809845ba42dc0bf12af7b9`) and plan (`eb03b0ede0a5f0aa0f7d89fe775d17b82d4420d1aad070f042aace4ddec8c81a`) PASS receipts. Independent compliance approves only this exact Step39 repair and leaves E0-5, B10, all wider Phase A debt and LSP49 open.
