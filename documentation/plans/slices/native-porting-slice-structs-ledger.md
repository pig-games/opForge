# Native Struct Slice Hypothesis Ledger

| ID | Hypothesis | Evidence for | Evidence against | Status | Next discriminator |
|---|---|---|---|---|---|
| S-001 | A typed struct literal is reaching the scalar expression bridge instead of native struct-instance capture. | `native_opcore_structs_fs_uae` fails on `Point { x: 24, y: 50 }` with `OTR921: expression bridge reported trailing text` during pass one. `opasmDriverRecordLabel` sends every `.const`/`.var`/`.set` operand through `readOperandValueForStatement`. | The initial routing change proves only struct declarations and fields are owned before ordinary processing. | confirmed | Add a native typed-instance capture boundary before `readOperandValueForStatement`, then rerun the same Level D test. |
| S-002 | Bounded struct capture can use `compile_values.upsertBindingV1` for the type-size key and direct member keys, without a second generic symbol table. | `cargo test -p asm native_struct_ -- --nocapture` passes after adding native type layout, `.byte`/`.word`/`.long` field widths, `captureTypedInstanceV1`, and pass-one/pass-two driver routing. The Level B source contract names `structs.captureTypedInstanceV1` and `compile_values.upsertBindingV1`; the Level C model continues to prove the canonical Point layout. | The host contract does not assemble or execute the 68020 package. | confirmed | Assemble the native package and repair any local assembly error before repeating Level D. |
| S-003 | The typed-instance implementation reaches the Level D canonical parity test. | The native package now assembles through the exact FS-UAE module graph; its direct build and the native formatter pass. The named Level D test was re-run after each local ABI repair. | All Level D attempts still fail during pass one with `OTR921: expression bridge reported trailing text`, proving typed capture returns nonzero before the scalar bridge fallback. The failure is unchanged after literal cursor preservation, case-insensitive type matching, and explicit D0 status branching. | blocked | Add one temporary Level E structured debug-contract event at each typed-capture reject boundary, preserving registers/SR/CCR/stack and avoiding service buffers; remove it after the first failed predicate is identified. |